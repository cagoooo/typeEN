# 📝 更新日誌 (v1.3.0)

## [1.3.0] - 2026-04-26

### ✨ 新功能（一次完成階段零 7 項）

把 v1.2.0 進階模式的學習鷹架完整化，這層做完學生與老師都能立刻感受到差別。

#### 0.1 初學者→進階主動引導
- 初學者模式結算頁，**完美通關**時直接秀「👉 立即試試進階模式（中排）」按鈕，一鍵進入。
- 連擊 ≥ 20 但有少量漏接也會出現較柔和的「要不要挑戰一下？」浮排按鈕。

#### 0.2 進階子目標解鎖系統
- 進階區段選擇器加入「依序解鎖」機制：中排 → 上排 → 下排 → 食指組。
- `'all'` 全鍵盤永遠可用，避免擋到能力較好的學生。
- 未解鎖區段顯示 🔒 鎖頭圖示與灰階樣式，點擊會用 toast 提醒「先完成上一階段練習」。
- 解鎖狀態存於 `gameStore.advancedSubsetUnlocked`，自動寫入本地加密快取。

#### 0.3 漏字驅動的個人化推薦
- 新增 `gameStore.missedLettersMap`，每局 ADVANCED 結算後累積漏接字母次數。
- 進階選擇器頂端新增「🎯 為你推薦：弱點補強」按鈕：取累積最常漏的 TOP 3 字母 + 同手指鄰居（依 `fingerMap`），自動組成 8 字內練習集。
- 個人化推薦不寫入排行榜（subsetId = 'personalized'）。

#### 0.4 進階模式專屬成就
- 新增三個成就：
  - `advanced_clear` 進階入門：首次完成全鍵盤通關
  - `advanced_perfect` 進階零漏接：全鍵盤通關且未用到寬限與漏接
  - `advanced_master` 進階大師：累計通關 10 次

#### 0.5 Mini DDA 自適應微調
- 進階模式內部偵測前 5 顆命中率，動態微調落速：
  - 5/5 全中 → +0.05 落速（推到 0.65–0.85）
  - 命中率 ≤ 40% → −0.05 落速（降到 0.55–0.75）
  - **永遠不超出 0.55–0.85** 友善範圍，且無 UI 提示（避免心理作用）。

#### 0.6 老師端「本週主推模式」
- 教師後台每個班級加入「本週主推模式」設定面板（mode + 進階子區段下拉）。
- 學生加入該班級後，主選單頂端顯示「📣 ⟪班級名⟫ 老師本週推薦：⟪模式⟫」橫幅 + 立即開始按鈕。
- Firestore `classes` doc 新增 `recommendedMode` / `recommendedSubsetId` 欄位。

#### 0.7 picker 鍵盤色卡視覺化
- 進階區段選擇器每個選項旁顯示 mini 鍵盤縮圖，該區段對應字母會以青綠色亮起。
- 全鍵盤選項與個人化推薦也有對應的鍵盤預覽，把抽象的「中排」具象化。

### 🛠️ 重構
- `AdvancedModePicker` 抽成獨立 component（`src/components/AdvancedModePicker.jsx`）。
- `GameArea.endGame` extraData 多帶 `firstMissUsed` 旗標，讓結算端能判定是否「真完美」（沒用到寬限）。
- `gameStore` 新增 `practiceSubsetId` 追蹤目前子集的 preset id（用於解鎖鏈與排行榜判定）。
- `userService` 新增 `setClassRecommendation` / `getClassRecommendation` 兩個 API。

## [1.2.0] - 2026-04-26

### ✨ 新功能（為三年級設計的難度橋樑）

- **新增「進階模式」橋接初學者與一般模式**
    - 過去從初學者跳一般模式是斷崖式落差（落速 ×3、出字頻率 ×4、引入炸彈與漏接扣血）。新模式落速固定 0.6–0.8、出字 1500–2000 ms、同屏上限 4 顆、**完全關閉炸彈與 HEAL 隨機字**，讓中年級學生有可預期的練習節奏。
    - 進階模式不會隨進度加速，方便老師帶班時掌握全班節奏。
- **首次漏接寬限**
    - 進階模式中，第一次漏接會顯示「寬限免扣血！」黃色提示，HP 不變。給三年級一次容錯機會，降低挫折感（連擊與震動仍照常觸發，因為的確是漏了）。
- **分區段練習（學習鷹架）**
    - 點「進階模式」會跳出區段選擇器：
        - 全鍵盤 (26 字母) — 唯一進排行榜
        - 中排 ASDFGHJKL（基準鍵入門）
        - 上排 QWERTYUIOP
        - 下排 ZXCVBNM
        - 食指組 RTFGVB / YUHJNM（左右食指分組）
    - 子區段練習通關條件等於該區段字母數（例如下排 7 字母通關），並 **不會寫入排行榜**，避免污染公平性。
- **進階模式排行榜分頁**
    - 排行榜新增「進階模式」分頁（青綠色主題），與其他模式採用相同的時間 ASC + 連擊 / 完成數備位排序。
    - Firestore stats schema 新增 `advancedTime / advancedCombo / advancedCompleted`，新舊用戶於下次同步時自動補齊欄位。

### 🛠️ 重構
- `gameStore` 新增 `practiceSubset` state；`startGame(mode, opts)` 支援第二個參數攜帶子集，並會在切到非進階模式時自動清空避免狀態殘留。
- `GameArea` 出字 / 完成判定 / 漏接處理三個分支統一加入 ADVANCED 條件，並抽出 `advancedPool` 變數涵蓋目標字數與補池邏輯。
- 教師後台 CSV 匯出新增「進階最佳(s)」欄位，順手修正既有「初管理者最佳」typo → 「初學者最佳」。

### 📘 設計理由
本次調整源於實際教學回饋：三年級學生在初學者模式表現良好後，跳到一般模式會在 30 秒內 HP 歸零，產生強烈挫折感。原一般模式並未調整（保留給程度足夠的學生），改以新增中間階梯方式解決，並佐以分區段練習與首次漏接寬限兩個鷹架元素，兼顧難度循序與失敗容錯。

## [1.1.11] - 2026-04-15

### ✨ 新功能（排行榜體驗四連發）
- **排行榜「我的排名定位 (Scroll to Me)」**
    - 打開排行榜後自動捲動到你所在的那一列，免得在 100 名裡瞎找自己。
    - 自己的列加上靛紫色發光邊框 + `YOU` 標籤 + 呼吸脈衝動畫。
    - 若自己那一列不在可視範圍，底部會浮出「📍 你在第 X 名」按鈕，一鍵再次定位。
- **排行榜空狀態引導 CTA**
    - 空榜單不再只有一句冷冷的文字，改為「還沒有人征服「一般模式」！」的鼓勵文案，加上一顆可直接開始挑戰該模式的大按鈕，讓第一位玩家就能成為榜首。
- **排行榜記憶體快取 (3 分鐘 TTL)**
    - `leaderboardService.getLeaderboard()` 加入 `Map` 快取，同一節課重複打開排行榜不再重複打 Firestore。
    - 新增 `invalidateLeaderboardCache(mode?)`，在玩家剛結算完有新成績時自動失效相關分頁，確保自己的新紀錄會立刻可見。
    - 預期可降低排行榜相關 Firestore 讀取量約 80–90%。

### 🛠️ 重構
- **本地資料 schema 版本化 (`crypto.js`)**
    - 新增 `__v` 版本信封包住加密 payload，並建立 `MIGRATIONS` 函式鏈。
    - v0→v1：自動為舊用戶補齊 `combo/completed` 預設值；v1→v2：預留各模式 `accuracy` 欄位。
    - 舊版資料讀取時會自動升級，不再因新欄位為 `undefined` 觸發隱性 bug。

## [1.1.10] - 2026-04-15

### 🐞 修正
- **排行榜資料缺失 (一般模式／初學者／單字)**
    - 修正 `App.jsx` 結算邏輯：未通關的玩家也會保存最佳「連擊數」與「完成字數」，僅有「通關時間」仍保留需完全通關才更新的嚴謹條件。
    - 重構 `leaderboardService.js`：改採「已通關 (time ASC)」+「有進度 (completed DESC)」兩段查詢合併後客戶端排序，並含 fallback 以防索引尚未建置。
    - 補齊 `ensureUserDocument`：新用戶建檔時即初始化完整的 `combo` / `completed` 欄位，避免 Firestore orderBy 漏掉舊文件。

### 🎨 UI/UX 優化
- **包容式排行顯示**
    - Leaderboard 列會依「已通關 / 未通關」分層顯示，未通關玩家顯示「未通關」標籤與較暗背景色，讓每個有在練習的學生都能在榜上看到自己的進度。
    - 排名顏色也會依是否通關調整，保留前三名的金銀銅強調給真正完成者。

### 📘 文件
- 全面改寫 `roadmap.md`：更新到 v1.1.10 已完成進度（含排行榜包容式顯示、每日任務、Gacha 等近期里程碑），並整理**階段一／二／三**的未來優化建議、已知問題待修清單與總結建議。

## [1.1.9] - 2026-03-21

### ✨ 新功能 (UX 優化)
- **強化手指提示 (HandsHint)**
    - 將提示文字加大 (`text-2xl`, `font-black`)，並採用高對比背景與微動效，大幅提升初學者辨識度。
- **排行變動回饋**
    - 遊戲結束後自動獲取當前排行，並與歷史最佳紀錄比對。
    - 顯示「上升了 X 名」或「⭐ 刷新最佳紀錄！」視覺回饋，提升競技成就感。
- **獎勵動效增強**
    - 在每日任務與每日登入領取獎勵時，新增金幣噴發浮動特效。
    - 遊戲結束畫面強化金幣獲得的視覺呈現。

### 🐞 修正
- **數據同步修正**
    - 確保各個模式（初學者、一般、無盡、單字）的最佳排行皆能正確儲存於本地與雲端。
- **排行榜服務修復**
    - 修正 `leaderboardService` 查詢邏輯，支援依據不同模式正確計算百分比與排名。

## [1.1.8] - 2026-03-21

### 🐞 修正
- **核心邏輯修復**
    - 修正了遊戲結束時 `getUserRank is not defined` 的 ReferenceError。
    - 確保 `App.jsx` 正確匯入 `leaderboardService` 中的排名計算函式。

## [1.1.7] - 2026-03-21

### 🛠️ 修正與優化
- **教師後台增強**
    - 修正了切換班級時學生名單未即時更新的問題。
    - 確保 `selectedClass` 變更時會主動刷新學生數據。
- **遊戲體驗優化**
    - 在遊戲結束畫面新增「全球排行榜」按鈕，方便玩家直接查看排名。
    - 修正了紀錄重置後，本地與雲端狀態同步的快取一致性問題。

## [1.1.6] - 2026-03-20

### 🐞 修正
- **遊戲畫面顯示修復**
    - 修復了背景 Canvas 在某些螢幕解析度或佈局下高度塌陷為 0 的問題。
    - 解決了字母（Letters）在遊戲中不可見的 Bug。
    - 優化了 `GameArea` 的全螢幕容器與 `z-index` 層級設定。

## [1.1.5] - 2026-03-19

### 🎨 UI/UX 優化
- **首頁捲動與 RWD 修復**
    - 徹底解決桌機版首頁無法捲動的問題，現在可以順利看到下方的「每日特工任務」。
    - 優化了 Start 畫面在行動裝置下的佈局，防止內容被裁切。
- **導師主控台優化**
    - 修復了「重置」按鈕在某些情況下被 CRT 特效層遮擋的問題。
    - 實作了 Grid/List 模式切換，並針對長姓名進行換行與折疊處理。
    - 新增自定義 `ConfirmModal` 取代原生彈窗，提升穩定性與視覺一致性。

## [1.1.4] - 2026-03-18

### ✨ 新功能
- **教師端：導師自我重設 (Self-Reset)**
    - 在教師後台新增「我的個人檔案」區塊，顯示導師目前的最高成績。
    - 提供「重設我的紀錄」按鈕，允許導師清除自己的測試數據，避免干擾學生排行榜。
- **教師端：重設學生成績**
    - 教師現在可以完全清除學生的遊戲紀錄（包括所有模式的秒數、連擊與完成次數）。
    - 此操作會同步將學生從全域排行榜中移除。
- **教師端：班級管理增強**
    - 支援將學生從特定的班級中移除。

### 🐞 修正
- **每日任務持久化**
    - 修正了每日任務進度無法正確同步至 Firestore 的問題。
    - 確保玩家在不同裝置登入時能看到一致的任務進度。
- **權限衝突解決**
    - 更新 Firestore 安全規則 (`firestore.rules`)，明確授權 `teacher` 角色能修改學生文件。
    - 解決了執行重設操作時出現的 `Missing or insufficient permissions` 錯誤。

### 🎨 UI/UX 優化
- 在教師後台左側面板加入更直觀的個人檔案顯示資訊。
- 為所有重設操作加入二次警告確認視窗，防止誤觸導致資料流失。
- 專案依賴與版本號同步更新至 `v1.1.4`。
