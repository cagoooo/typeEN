# 🚀 Neon Typer (打字特工) - 進度與未來發展藍圖

> 📅 最後更新：v1.2.0 (2026-04-26)

這份文件總結了目前專案已完成的核心進度，並提供未來可以繼續優化、開發的具體建議與方向。

---

## 🆕 v1.2.0 重點：難度曲線與學習鷹架

> **動機**：實際教學回饋顯示，三年級學生從初學者模式跳到一般模式有斷崖式落差（落速 ×3、出字頻率 ×4，並引入炸彈與漏接扣血）。本版以「插入中間階梯 + 增加容錯」雙策略解決，原一般模式保留給程度足夠的學生。

| 項目 | 落速 | 出字間隔 | 同屏上限 | 炸彈 | 漏接懲罰 |
|---|---|---|---|---|---|
| 初學者 | 0.3–0.5 | 2500–4000 ms | 3 | ❌ | 不扣血 |
| **🆕 進階模式** | **0.6–0.8（不加速）** | **1500–2000 ms** | **4** | **❌** | **首次免扣，之後 −1 HP** |
| 一般模式（初始） | 1.0–1.4 | 600–1200 ms | 無 | ✅ 8% | −1 HP |
| 一般模式（30字後） | 2.8–3.2 | 300–600 ms | 無 | ✅ 8% | −1 HP |

**新增能力**：
- **進階模式**主選單獨立按鈕（青綠色），有獨立排行榜分頁。
- **首次漏接寬限**：進階模式中第一次漏接顯示「寬限免扣血！」HP 不變，給三年級一次容錯。
- **分區段練習**：點進階模式跳出選擇器：全鍵盤 / 中排 / 上排 / 下排 / 食指組。子區段練習**不寫入排行榜**避免污染公平性。
- **教師後台 CSV 匯出**新增「進階最佳(s)」欄位。

---

## ✅ 目前已完成的核心機制與進度 (截至 v1.2.0)

### 1. 🏅 核心遊戲與模式
* **初學者模式**：固定且緩慢的掉落速度，提供指法方向提示（上移/下移），並含「最常漏字母」回饋；HP 歸零會立刻結算，防止學生亂按拖時間。
* **🆕 進階模式 (v1.2.0)**：橋接初學者→一般模式的中間階梯。固定 0.6–0.8 落速、1500–2000 ms 出字、同屏 4 顆上限、無炸彈干擾。**首次漏接寬限**機制 + **分區段練習**（中排/上排/下排/食指組）讓三年級學生有可預期的練習節奏。只有「全鍵盤」區段會進排行榜。
* **一般模式**：隨進度加快掉落，含炸彈與補血道具；v1.1.10 起即使未通關，仍會記錄最佳連擊與完成數，讓學生的進度都看得到。
* **無盡生存模式**：挑戰撐過最長時間，每 45 秒出現 **BOSS 挑戰**；成就系統會偵測 60 秒存活等里程碑。
* **單字挑戰模式**：練習常見基礎單字的連續拼打，10 單字為一輪。
* **劇情戰役 (Campaign Map)**：多關卡任務（存活時間、擊破數、BOSS 擊殺等），通關解鎖下一關，搭配 `StoryCutscene` 劇情演出。

### 2. 🛡️ 完善的防作弊與教學機制
* **強制鍵盤輸入**：移除滑鼠點擊觸發字母，只能透過正確的鍵盤按鍵。
* **亂按懲罰 (-1 HP)**：按錯無效鍵觸發扣血，防止矇混過關。
* **最常漏字母回饋**：初學者模式結算貼心列出漏打字母，作為補強參考。
* **虛擬鍵盤指法提示 (HandsHint)**：畫面下方大字體、高對比、含上下移提示的手指對應色塊（v1.1.9 強化）。

### 3. ☁️ 雲端同步與帳號系統 (Firebase)
* **Google 帳號登入/登出**：解決初次登入的非同步 Race Condition，搭配本地 `userProfile` 快取避免閃爍。
* **資料庫同步**：本地/雲端最佳成績雙向合併（以最短時間、最高連擊為準），換裝置也能延續紀錄。
* **虛擬貨幣與商城**：金幣獎勵 + 背景/特效/BGM 購買；Fever Time（16:00–17:00）雙倍金幣；「加倍券」道具延長效果。
* **每日登入連擊**：連續登入獎勵（base 10 + 每日 +5，上限 +50），搭配浮動金幣動畫回饋。
* **每日任務系統**：三項日常任務（指定模式完成局數、準確率、連擊數），達成可領取金幣。
* **成就大廳 (Achievements)**：連擊里程碑、存活達標、累計完成、商城消費等多類徽章，雲端同步。
* **Gacha 抽獎**：消耗金幣抽頭像 / 稱號 / 頭像框 / 消耗品（稀有度分層）。
* **消耗品系統**：護盾、時停 3 秒、金幣加倍券。

### 4. 👩‍🏫 導師與班級系統
* **長按解鎖導師權限**：頭像長按 3 秒輸入密碼（`VITE_TEACHER_SECRET`），配合進度條動畫提示。
* **班級管理**：導師建立班級獲得 6 碼代碼，學生輸入代碼或點擊分享連結即可加入。
* **導師儀表板**：即時查看班級內所有學生最高分、連擊、遊玩時間；支援 Grid / List 切換；長姓名會折疊。
* **導師工具**：移除學生、重置學生成績（同步從排行榜移除）、自我重置測試紀錄（不污染學生排名）。
* **班級切換修正**：v1.1.7 修正切換班級時學生名單未更新的問題。

### 5. 🔗 社交與分享功能
* **成績單分享 (Share Profile)**：產出個人成績網址（QR Code），支援訪客模式瀏覽。
* **結果分享卡 (ResultShareCard)**：遊戲結束後匯出成績圖，適合社群分享。
* **URL 參數自動處理**：`?uid=` 自動進入分享檢視、`?classCode=` 登入後自動加入班級。

### 6. 🏆 排行榜與回饋機制 (v1.1.9 → v1.2.0 強化)
* **全球英雄榜**：**🆕 五模式獨立排行 (v1.2.0)**（初學者／**進階**／一般／無盡／單字），含頭像、邊框、稱號顯示。
* **排行變動通知**：結算時比對歷史最佳，顯示「上升 X 名」或「⭐ 刷新最佳紀錄！」。
* **🆕 包容式排行顯示 (v1.1.10)**：一般／初學者／單字模式排行榜不再只顯示「已通關」玩家，**有進度但未通關**（有連擊或部分完成）的玩家也會列入榜單；UI 以「未通關」標籤與暗色列區分。
* **🆕 完整 stats 初始化 (v1.1.10)**：新用戶建檔時即寫入 `combo/completed` 預設值，避免排行索引遺漏舊文件。
* **🆕 子集練習隔離 (v1.2.0)**：進階模式的區段練習（單行 / 食指組）只記錄本地進度，不上傳排行榜，避免「練 7 個下排字母」與「打完 26 個字母」放在同個榜單比較。

### 7. 🎧 音訊與 PWA 基礎
* **10 首熱血科幻 BGM**：含 `bgm_auto` 自動切換，使用者可於首頁選單切換。
* **Howler 音訊引擎**：打擊聲、命中、失敗、完美音效分類。
* **Service Worker (sw.js)**：JS/CSS 採 Network-First，含 `skipWaiting` 讓新版本立即生效；修正過對 `chrome-extension://` 請求的 Cache API 崩潰。

---

## 🔮 未來優化與改良建議藍圖

> **目錄**
> - [🌱 階段零（v1.2 後續迭代）：把 B+D 路線完整化](#-階段零v12-後續迭代把-bd-路線完整化)
> - [🎯 階段一：教學成效與數據深化（短期優先）](#-階段一教學成效與數據深化短期優先)
> - [📚 階段一·B：完整教學流程設計（課程化）](#-階段一b完整教學流程設計課程化)
> - [🎮 階段二：遊戲化與互動性擴張（中期目標）](#-階段二遊戲化與互動性擴張中期目標)
> - [💻 階段三：架構、效能與長期擴充（長期願景）](#-階段三架構效能與長期擴充長期願景)

---

### 🌱 階段零（v1.2 後續迭代）：把 B+D 路線完整化

> 這層的工作都是直接延伸 v1.2.0 的進階模式 + 分區段練習機制，**做下去能立刻被三、四年級老師感受到**。建議優先排這一層，再往上做階段一的數據深化。

#### 0.1 從初學者「主動引導」到進階模式
**問題**：學生在初學者模式拿了高分後，不一定知道下一步該玩進階模式，可能直接挑戰一般模式被打趴。
* **概念**：初學者模式結算頁面，若**完美通關（零漏接）+ 連擊 ≥ 20**，自動彈出引導 banner：「你已經準備好挑戰進階模式了！👉 立即試試（中排）」並附直達按鈕。
* **實作提示**：`App.jsx` 結算畫面的 BEGINNER 區塊（line ~795）已有「完美通關提示」，加一顆按鈕直接呼叫 `startGame('ADVANCED', { subset: PRACTICE_SUBSETS[1].letters })` 即可。
* **預期效益**：減少老師班級「集體卡在初學者」的情形。

#### 0.2 進階模式內的子目標解鎖系統
**問題**：目前進階模式選單一次秀 5 個區段，學生可能直接跳全鍵盤又被打趴。
* **概念**：採「依序解鎖」設計：
  1. 預設只解鎖「中排 ASDFGHJKL」
  2. 中排通關 1 次 → 解鎖「上排 QWERTYUIOP」
  3. 上排通關 1 次 → 解鎖「下排 ZXCVBNM」
  4. 三排都通關過 → 解鎖「食指組」與「全鍵盤」
* **實作提示**：在 `gameStore` 新增 `advancedSubsetUnlocked: ['home']` 並在區段 picker 中以鎖頭圖示標示未解鎖選項；通關 ADVANCED 子集時呼叫對應 unlock action。
* **延伸**：解鎖時播放短特效與「🎉 解鎖：上排訓練」浮動文字，給學生小成就感。

#### 0.3 漏字驅動的個人化推薦區段
**問題**：學生在進階「中排」漏 R/T 很多次，下次卻可能還是選回中排重練，沒針對弱點。
* **概念**：根據累積的 `missedLetters` 統計，在進階模式選單上方多顯示一個「🎯 為你推薦」按鈕，自動依弱點動態組成 5–8 字母的子集（例如「漏最多的 R/T 加上鄰近 F/G/V/B」）。
* **實作提示**：
  * `userService.js` 加入 `stats.missedLettersMap` 累積各字母漏接次數
  * 推薦演算法：取漏接次數 TOP 3 字母，再以 `fingerMap` 拉同手指的 2–3 個鄰居組成練習集
  * 子集 id 用 `personalized_<timestamp>`，視為一次性練習集不寫入排行榜
* **預期效益**：每位學生看到的進階模式略有不同，自動往弱點補課。

#### 0.4 進階模式專屬成就
**問題**：目前成就系統有 `beginner_pro`（初學者 100 連擊）但沒有進階模式的，少了里程碑。
* **概念**：新增三個進階成就：
  * `advanced_clear`：首次完成進階模式全鍵盤通關
  * `advanced_perfect`：進階全鍵盤完美通關（零漏接，含寬限不算）
  * `advanced_master`：進階模式累積通關 10 次
* **實作提示**：`constants.js` 的 `ACHIEVEMENTS` 陣列新增三筆，`gameStore.js` 的 `checkAchievements` 加判斷即可。`firstMissUsed` 已可區分「真完美」（沒用到寬限）與「靠寬限通關」。

#### 0.5 進階模式內的自適應微調 (Mini DDA)
**問題**：固定 0.6–0.8 落速對所有學生一視同仁，但同一班內仍有程度差。
* **概念**：保留進階模式整體節奏，但在範圍內動態微調：
  * 偵測前 5 顆全部命中 → 落速微調至 0.7–0.85
  * 偵測前 5 顆漏 ≥ 2 顆 → 落速微調至 0.55–0.7
  * 永遠不超出進階模式的「友善範圍」
* **實作提示**：`GameArea.jsx` 的 `spawnLetter` 中 ADVANCED 分支加入 `engineState.current.recentHits` 滑窗，按比例調整 `speed`。
* **設計原則**：不告訴學生在自適應，避免心理作用；老師端可選擇關閉。

#### 0.6 老師端：班級分配模式
**問題**：老師目前無法統一指定班級「這週都打進階模式」。
* **概念**：在 `TeacherDashboard.jsx` 新增「本週主推模式」設定，學生加入該班且打開遊戲時，主選單頂端會顯示「✨ 本週老師推薦：進階模式（中排）」浮動提示與快速啟動鈕。
* **實作提示**：`classes/{classId}` 加入 `recommendedMode` + `recommendedSubset` 欄位，主選單在登入後讀取顯示。
* **延伸**：老師可看到「班級內有多少人完成本週推薦」的進度條。

#### 0.7 鍵盤色卡 + 區段對應視覺化
**問題**：學生選了「上排 QWERTYUIOP」時，不一定立刻意識到對應到鍵盤上的位置。
* **概念**：選擇器的每個區段選項旁顯示一張小型鍵盤縮圖，該區段對應的鍵亮起。
* **實作提示**：`HandsHint.jsx` 已有完整鍵盤渲染，抽出一份輕量版（不含手指），在 picker modal 中渲染並依 `subset.letters` 高亮對應鍵。
* **教育價值**：把抽象的「中排」具象化為視覺對應，符合中年級學生的認知特性。

#### 0.8 「指法防呆」可選模式
**問題**：學生可以一指神功打過進階模式，違反指法練習目的。
* **概念**：在進階模式選擇器底部加上 checkbox「🤚 指法強制模式」（預設關），勾起來後若按鍵時間距前一鍵 < 100 ms 且都用同一根手指可達範圍，視為作弊不計分。
* **實作提示**：難度較高，需要紀錄前一鍵 timestamp + 該鍵應對應的 `fingerMap`，並推測學生是否使用了同一根手指。建議先做最簡版：「同一根手指連續按超過 3 鍵則警告」。
* **謹慎度**：這項可能挫傷學生信心，建議只在 `firstMissUsed` 機制成熟後再加，並由老師端打開。

---

### 🎯 階段一：教學成效與數據深化（短期優先）

1. **錯字/易錯鍵盤熱區圖 (Heatmap)**
   * **概念**：把 `missedLetters` 資料視覺化成鍵盤熱區，紅色代表常漏、綠色代表熟練。
   * **延伸**：個人儀表板顯示自己的熱區，導師儀表板可看「全班平均熱區」，一眼知道本週要補強哪些按鍵。
   * **實作提示**：`GameArea.jsx` 已在 `engineState.current.missedLetters` 累積資料，只要在結算時同步到 `userService.js` 的 `stats.missedLettersMap` 即可累積。

2. **WPM (Words Per Minute) 與 CPM (Chars Per Minute) 統計**
   * **概念**：加入國際標準指標，特別是單字挑戰模式。
   * **實作提示**：`completedCount / (gameTime / 60)` 就是 CPM；WPM 可用 CPM/5。加到 `handleGameEnd` 結算後同步到 `stats.wpmBest`。

3. **匯出班級成績報表 (CSV / Excel)**
   * **概念**：導師儀表板新增「匯出報表」按鈕，產生 UTF-8 BOM CSV。
   * **欄位**：學號、姓名、各模式最佳時間、最大連擊、完成字數、總遊玩時間、最常漏字母 TOP 5。
   * **實作提示**：`TeacherDashboard.jsx` 已有學生資料，加上 Blob + `URL.createObjectURL` 即可下載。

4. **準確率即時計算與存檔**
   * **概念**：目前準確率只在每日任務中估算。建議把「每局準確率」計入 stats，排行榜加入第四欄。
   * **實作提示**：`handleGameEnd` 的 `accuracy` 已計算，但沒存入 stats。同步到 `stats.{mode}Accuracy` 即可。

5. **學習曲線圖（個人成長軌跡）**
   * **概念**：個人成就大廳加入「最近 30 局」折線圖，展示時間 / 連擊變化。
   * **實作提示**：新增 `history` 子集合（`users/{uid}/history/{timestamp}`），每局結算時寫入；用 SVG 或 Chart.js 繪製。

6. **導師自訂題庫與單字包**
   * **概念**：導師在儀表板建立「本週單字包」，學生端單字挑戰優先掉落老師指定的單字。
   * **實作提示**：`classes/{classId}/wordSets/{setId}` 子集合，`GameArea.jsx` 啟動 WORD 模式時先檢查加入的班級是否有 active 題庫。

---

### 📚 階段一·B：完整教學流程設計（課程化）

> 把現有的五個模式 + 進階子區段串成一個 **6–12 週的完整課程**，讓老師可以直接按表操課。這層的價值不在新功能，而在「對教學流程的明確指引」。

#### 1.B.1 內建課程模板：6 週入門 / 12 週完整版

**6 週入門（適合學期下半段補打字課）**：
| 週次 | 模式 | 目標 | 評量標準 |
|---|---|---|---|
| 1 | 初學者 | 認識指法 + 26 字母位置 | 完成 3 局，HP 不歸零 |
| 2 | 進階 - 中排 | 熟悉 8 個基準鍵 | 通關時間 < 30s，連擊 ≥ 5 |
| 3 | 進階 - 上排 | 上排 10 字母定位 | 通關時間 < 40s，連擊 ≥ 8 |
| 4 | 進階 - 下排 | 下排 7 字母收尾 | 通關時間 < 25s，零漏接 |
| 5 | 進階 - 全鍵盤 | 整合所有區段 | 完成全鍵盤進階通關 |
| 6 | 一般模式 | 實戰節奏挑戰 | 自由挑戰，建立排行榜目標 |

**12 週完整版**：在每兩週插入一週「複習 + 競賽」（單字挑戰 + 班級內比賽），並在第 10 週後引入無盡生存與劇情戰役。

* **實作提示**：先以**靜態 JSON** 寫成課程模板，存在 `src/data/curriculum.json`；`App.jsx` 主選單頂端加「📅 本週課程：第 3 週 - 進階上排」。後續可演進為老師端可編輯。

#### 1.B.2 課程模式 (Curriculum Mode)：學生端任務頁

* **概念**：在主選單加上「📚 我的課程」分頁，顯示老師指定的課程進度條，每週都有對應任務。
* **流程範例**：
  1. 學生開遊戲 → 看到「本週任務：完成進階上排 3 次，連擊 ≥ 8」
  2. 完成 1 次 → 進度條 1/3 + 浮動「+1 完成」
  3. 完成 3 次 → 自動解鎖下週任務 + 獎勵 100 金幣
* **資料模型**：`users/{uid}/curriculumProgress/{week}` 子集合，記錄 `completed`、`bestTime` 等。
* **教育價值**：把「練打字」從自由放牧變成有節奏的學習路徑，符合課綱要求。

#### 1.B.3 班級進度儀表板（老師視角）

* **概念**：老師儀表板新增「課程進度」分頁，以 grid 方式顯示班上每位學生在每週任務的完成狀態（✅ 完成 / 🟡 進行中 / ❌ 未開始）。
* **價值**：老師一眼看出哪幾位學生卡住，可以個別輔導；也能在班親會直接秀出整體完成率。
* **延伸**：點任一格進入該學生詳細紀錄，看到 missedLetters TOP 5、平均速度等。

#### 1.B.4 結業證書 PDF 列印

* **概念**：完成 6 週課程的學生可下載個人化「打字結業證書」PDF，含學生姓名、完成日期、最佳成績、班級資訊。
* **設計**：採前一段 `pdf-export-print-best-practice` 的做法 — `window.print()` + `@media print` CSS，不依賴 html2pdf。
* **教育價值**：可實體印出貼班級榮譽榜，或附在學期成績單上，給家長看到具體成果。
* **實作提示**：模板用一頁 A4 橫式 + 浮水印背景；中文字型用 Noto Sans TC（從先前 OG 圖技能那條線整合過來）。

#### 1.B.5 段考 / 期末打字檢測模式

* **概念**：開放老師建立「打字檢測」session，學生在指定時間進入後執行：
  1. 打 26 字母進階模式 1 局
  2. 打單字挑戰 1 局
  3. 系統自動計算 WPM / 準確率 / 完成度
  4. 結果僅老師可見（不上排行榜）
* **資料模型**：`classes/{classId}/assessments/{sessionId}`，含開放時間區間、學生提交紀錄。
* **價值**：把這款遊戲升格成「能直接做評量」的工具，老師打字技能評分有客觀依據。

---

### 🎮 階段二：遊戲化與互動性擴張（中期目標）

1. **多人在線競技模式 (1v1 或 班級大亂鬥)**
   * **概念**：Firebase Realtime DB 配對對手，上下分割畫面顯示雙方進度；滿 Perfect Combo 可丟干擾炸彈。
   * **價值**：極大化學生練習動機，並與社交學習結合。

2. **班級排行榜（過濾模式）**
   * **概念**：加入班級後，榜單多一個「我的班級」分頁，只顯示同班同學。
   * **實作提示**：`leaderboardService.getLeaderboard` 加上 `classId` 參數，用 `where('joinedClasses', 'array-contains', classId)` 過濾。

3. **週榜 / 月榜**
   * **概念**：除了全時排行，加入 7 天、30 天榜單，避免老玩家永遠壟斷榜單。
   * **實作提示**：每局結算寫入 `history` 時記錄 `createdAt`，週榜用 `where('createdAt', '>=', oneWeekAgo)` 查詢。

4. **自適應難度曲線 (DDA)**
   * **概念**：偵測學生連續死亡就暗中放慢 10% 掉落；連續 Combo 就加速或加干擾物。
   * **實作提示**：`GameArea.jsx` 的 `spawnLetter` 增加 `difficultyModifier` 倍率，由結算紀錄的死亡/連擊動態調整。

5. **任務鏈 / 賽季制 (Battle Pass)**
   * **概念**：每月一個「賽季」，含 10 項任務（累計連擊 500、累計打字 1000 個、特定關卡通關等），達成解鎖限定頭像 / 稱號。
   * **價值**：延長遊戲生命週期，給已精通打字的老手新動機。

6. **頭像外框動態升級**
   * **概念**：隨排名變動，第一名自動獲得金色動態外框（電鍍流光效果）。
   * **實作提示**：目前已有 `appearance.border` 系統，加入 `rank_champion_animated` CSS 動畫類別即可。

### 💻 階段三：架構、效能與長期擴充（長期願景）

1. **PWA 離線遊玩完整化**
   * **概念**：完善 `manifest.json` + `sw.js`，讓學生能「安裝到桌面」並離線玩單機模式。
   * **現況**：目前 sw.js 已採 Network-First 策略，但離線回放、離線模式資料同步還沒實作。
   * **實作建議**：離線時 queue 成績到 IndexedDB，網路恢復時 flush 到 Firestore。

2. **Bundle 分割與懶加載**
   * **現況**：build 後主 JS 達 1.2MB（gzip 319kB），已出現 chunk 警告。
   * **建議**：用 `React.lazy` 延遲載入 `TeacherDashboard`、`Shop`、`AchievementDashboard`、`CampaignMap`、`Leaderboard`；firebase 的 `firestore` 模組也可動態分割。

3. **Firestore 費用優化**
   * **現況**：每次開排行榜就打兩個 `limit(100)` 查詢（v1.1.10 新增），多人頻繁開合會累積讀取費用。
   * **建議**：加入 5 分鐘的 client-side 快取（sessionStorage + 時戳），避免同一使用者短時間內重複查詢。

4. **Firestore 安全規則強化**
   * **現況**：`users` 允許 `auth.uid == userId` 或 `teacher` 寫入，但沒有驗證 `stats` 欄位的合法性。
   * **建議**：加上 `request.resource.data.stats.normalTime is number && request.resource.data.stats.normalTime >= 0` 等類型檢查，避免偽造 -999 的超強分數。

5. **A11y（無障礙）支援**
   * **概念**：鍵盤焦點管理、螢幕閱讀器語意、高對比模式切換。
   * **實作提示**：Modal 加上 `role="dialog"` `aria-labelledby`；遊戲面板的倒數、HP 用 `aria-live` 即時報讀。

6. **國際化 (i18n)**
   * **概念**：目前介面全中文；若想讓其他語言背景的學生也能使用，抽出所有字串到 `locales/zh-TW.json` / `en.json` / `ja.json`。
   * **實作建議**：使用 `react-i18next` 或自製 `useTranslation` hook。

7. **單元測試與 E2E 測試**
   * **現況**：無測試套件。關鍵邏輯如 `syncStatsToCloud` 的合併、`leaderboardService` 的排序規則、`checkAchievements` 分支很值得加單元測試。
   * **建議**：vitest + @testing-library/react 做邏輯測試；Playwright 做首頁登入→玩一局→看排行的 e2e 流程。

8. **數據埋點與分析**
   * **概念**：用 Firebase Analytics 或 Plausible 追蹤：哪個模式最多人玩、卡關哪個關卡、商城裡學生最愛兌換哪件外觀。
   * **價值**：用真實使用數據決定未來要先強化哪個模組，避免憑感覺開發。

---

## 🐞 已知問題 / 待修清單

| 編號 | 問題描述 | 建議優先序 |
|---|---|---|
| 1 | `firestore.rules` 未驗證 `stats` 欄位型別，存在偽造排行可能 | 中 |
| 2 | Bundle size 1.2MB 未做 code-splitting，首次載入較慢 | 中 |
| 3 | 離線模式尚未實作資料佇列 | 低 |
| 4 | 排行榜無「班級內」與「時間區間」篩選 | 高 |
| 5 | 成就無進度條（只有達成 / 未達成） | 低 |
| 6 | 沒有單元測試，每次改 leaderboard 邏輯只能 build 驗證 | 中 |
| 7 | 進階模式無專屬成就，少了里程碑（已列入 0.4） | 中 |
| 8 | 學生在初學者模式拿高分後沒有自動引導去進階模式（已列入 0.1） | 高 |
| 9 | 老師儀表板顯示時還沒納入 `advancedTime`/`advancedCombo` 欄位（CSV 已加） | 中 |

---

## 💡 總結建議

目前的《Neon Typer》已經是一個完整、具備教育意義的免費教學工具，核心機制非常扎實。**v1.2.0 補上了三年級學生最關鍵的難度橋樑**，下一步建議優先把這條路線完整化。

### 推薦下一步開發順序（依「教學影響力 vs 開發成本」排序）

| 優先度 | 項目 | 預估工時 | 教學影響力 |
|---|---|---|---|
| 🔥 高 | 0.1 初學者→進階引導 banner | 0.5 天 | ⭐⭐⭐⭐⭐ |
| 🔥 高 | 0.4 進階模式專屬成就 | 0.5 天 | ⭐⭐⭐⭐ |
| 🔥 高 | 0.2 子目標解鎖系統 | 1 天 | ⭐⭐⭐⭐ |
| 🟡 中 | 0.7 鍵盤色卡視覺化 | 1 天 | ⭐⭐⭐ |
| 🟡 中 | 1.B.1 課程模板（靜態） | 1 天 | ⭐⭐⭐⭐ |
| 🟡 中 | 1（階段一）熱區圖 | 2 天 | ⭐⭐⭐⭐ |
| 🟡 中 | 0.3 漏字驅動的個人化推薦 | 2 天 | ⭐⭐⭐⭐⭐ |
| 🟢 低 | 0.5 進階 Mini DDA | 2 天 | ⭐⭐⭐ |
| 🟢 低 | 0.6 老師班級分配模式 | 3 天 | ⭐⭐⭐⭐ |
| 🟢 低 | 1.B.2 課程模式（資料庫） | 3 天 | ⭐⭐⭐⭐⭐ |
| 🟢 低 | 1.B.4 結業證書 PDF | 1 天 | ⭐⭐⭐ |
| 🟢 低 | 0.8 指法防呆 | 3 天 | ⭐⭐ |

### 三條建議路線（依老師需求挑一條走）

**🅰️ 「立刻就能感受到改變」路線（兩天工時）**
0.1 + 0.4 + 0.2 → 三項都偏 UX，做完學生下次打開遊戲就能看到引導 banner、子集解鎖動畫與新成就，**最小成本最大感受**。

**🅱️ 「期中考前能展示給家長看」路線（一週工時）**
階段一的「熱區圖 + WPM 統計 + CSV 匯出強化」+ 0.3 漏字驅動推薦 → 給老師具體的教學回饋工具，可在家長日直接秀「您家孩子最常漏哪幾個字母 + 個人化練習計畫」。

**🅲️ 「整學期課程化」路線（兩週工時）**
1.B.1 課程模板 + 1.B.2 課程模式 + 1.B.3 班級進度儀表板 + 1.B.4 證書 PDF → **把這款遊戲變成老師可以直接拿來上一整學期資訊課的教材**。最有戰略價值，也是最能說服其他老師導入的賣點。

---

### 一句話總結

> v1.2.0 解決了「進不去」的問題（難度斷崖），下一階段要解決「停不下來」的問題（學習動機）與「老師敢用」的問題（教學流程明確）。優先做階段零（0.1 / 0.4 / 0.2）拿到立刻可見的回饋，再往階段一·B 課程化推進，對老師與學生雙邊的價值會最大化。
